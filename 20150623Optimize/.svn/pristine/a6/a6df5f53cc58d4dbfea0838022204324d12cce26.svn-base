CREATE TABLE [dbo].[LotteryRecord] (
    [id]               BIGINT   IDENTITY (1, 1) NOT NULL,
    [lotteryDate]      DATETIME NULL,
    [preorder19dianId] BIGINT   NOT NULL,
    CONSTRAINT [PK_LotteryRecord] PRIMARY KEY CLUSTERED ([id] ASC)
);

