CREATE TABLE [dbo].[EmployeeRole] (
    [EmployeeRoleID]     INT IDENTITY (1, 1) NOT NULL,
    [EmployeeID]         INT NULL,
    [RoleID]             INT NULL,
    [EmployeeRoleStatus] INT NULL,
    CONSTRAINT [PK_EmployeeRole] PRIMARY KEY CLUSTERED ([EmployeeRoleID] ASC)
);

