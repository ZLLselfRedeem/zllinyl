CREATE TABLE [dbo].[DishConnMaterial] (
    [ID]              INT        IDENTITY (1, 1) NOT NULL,
    [materialID]      INT        NULL,
    [dishPriceI18nID] INT        NULL,
    [unit]            INT        NULL,
    [quantity]        FLOAT (53) NULL,
    CONSTRAINT [PK_DishConnMaterial] PRIMARY KEY CLUSTERED ([ID] ASC)
);

