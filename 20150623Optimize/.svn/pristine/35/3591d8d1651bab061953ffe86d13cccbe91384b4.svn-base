CREATE TABLE [dbo].[MapInfo] (
    [mapId]     INT           IDENTITY (1, 1) NOT NULL,
    [mapName]   NVARCHAR (50) NULL,
    [mapStatus] INT           NULL,
    CONSTRAINT [PK_MapInfo] PRIMARY KEY CLUSTERED ([mapId] ASC)
);

