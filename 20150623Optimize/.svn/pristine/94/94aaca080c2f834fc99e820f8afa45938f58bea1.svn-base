CREATE TABLE [dbo].[DishOptionType] (
    [id]           INT           IDENTITY (1, 1) NOT NULL,
    [optionName]   NVARCHAR (50) NULL,
    [internalName] NVARCHAR (50) NULL,
    CONSTRAINT [PK_DishOptionType] PRIMARY KEY CLUSTERED ([id] ASC)
);

