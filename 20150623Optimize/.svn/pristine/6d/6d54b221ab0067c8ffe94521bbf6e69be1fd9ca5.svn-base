CREATE TABLE [dbo].[DishConnType] (
    [DishConnTypeID]     INT IDENTITY (1, 1) NOT NULL,
    [DishID]             INT NULL,
    [DishTypeID]         INT NULL,
    [DishConnTypeStatus] INT NULL,
    CONSTRAINT [PK_DishConnType] PRIMARY KEY CLUSTERED ([DishConnTypeID] ASC)
);

