CREATE TABLE [dbo].[ShopCoordinate] (
    [shopCoordinateId] INT        IDENTITY (1, 1) NOT NULL,
    [shopId]           INT        NULL,
    [mapId]            INT        NULL,
    [longitude]        FLOAT (53) NULL,
    [latitude]         FLOAT (53) NULL,
    CONSTRAINT [PK_ShopCoordinate] PRIMARY KEY CLUSTERED ([shopCoordinateId] ASC)
);

