CREATE TABLE [dbo].[PrePayPrivilegeFrequency] (
    [id]                   BIGINT   IDENTITY (1, 1) NOT NULL,
    [prePayPrivilegeId]    INT      NULL,
    [achieveActivistsTime] DATETIME NULL,
    [customerID]           BIGINT   NULL,
    CONSTRAINT [PK_PrePayPrivilegeFrequency] PRIMARY KEY CLUSTERED ([id] ASC)
);

