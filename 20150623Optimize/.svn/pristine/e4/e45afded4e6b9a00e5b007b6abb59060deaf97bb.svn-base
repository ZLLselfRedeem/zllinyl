using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Data;
using VAGastronomistMobileApp.Model;
using VAGastronomistMobileApp.SQLServerDAL;
namespace VAGastronomistMobileApp.WebPageDll
{
    /// <summary>
    /// 功能描述:菜的配料
    /// 创建标识:罗国华 20131030
    /// </summary>
    public class DishIngredientsOperate
    {
        public static DishIngredients GetModel(int ingredientsId)
        {
            return DishIngredientsManager.GetModel(ingredientsId);
        }

        public static bool Insert(DishIngredients model)
        {
            return DishIngredientsManager.Insert(model);
        }

        public static bool Del(int ingredientsId)
        {
            return DishIngredientsManager.Del(ingredientsId);
        }

        public static bool Update(DishIngredients model)
        {
            return DishIngredientsManager.Update(model);
        }
    }
}