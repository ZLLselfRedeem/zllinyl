CREATE TABLE [dbo].[CustomerCheckedShop] (
    [Id]         BIGINT   IDENTITY (1, 1) NOT NULL,
    [customerId] BIGINT   NULL,
    [companyId]  INT      NULL,
    [shopId]     INT      NULL,
    [checkTime]  DATETIME NULL,
    [cityId]     INT      NULL,
    CONSTRAINT [PK_CustomerCheckedShop] PRIMARY KEY CLUSTERED ([Id] ASC)
);

