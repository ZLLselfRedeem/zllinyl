CREATE TABLE [dbo].[MenuConnCompany] (
    [menuCompanyId] INT IDENTITY (1, 1) NOT NULL,
    [menuId]        INT NULL,
    [companyId]     INT NULL,
    [status]        INT NULL,
    CONSTRAINT [PK_MenuConnCompany] PRIMARY KEY CLUSTERED ([menuCompanyId] ASC)
);

