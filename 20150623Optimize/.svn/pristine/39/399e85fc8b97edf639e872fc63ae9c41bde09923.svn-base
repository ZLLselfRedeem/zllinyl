CREATE TABLE [dbo].[DishOptionDetail] (
    [id]               INT           IDENTITY (1, 1) NOT NULL,
    [optionTypeId]     INT           NULL,
    [optionDetailName] NVARCHAR (50) NULL,
    CONSTRAINT [PK_DishOptionDetail] PRIMARY KEY CLUSTERED ([id] ASC)
);

