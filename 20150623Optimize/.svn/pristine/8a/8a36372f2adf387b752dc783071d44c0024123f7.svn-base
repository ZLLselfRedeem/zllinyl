CREATE TABLE [dbo].[CustomerLoginInfo] (
    [customerLoginId] BIGINT         IDENTITY (1, 1) NOT NULL,
    [customerId]      BIGINT         NULL,
    [loginTime]       DATETIME       NULL,
    [uuid]            NVARCHAR (100) NULL,
    [loginCityId]     INT            NULL,
    CONSTRAINT [PK_CustomerLoginInfo] PRIMARY KEY CLUSTERED ([customerLoginId] ASC)
);

