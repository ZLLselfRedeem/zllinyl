CREATE TABLE [dbo].[ApplyMapping] (
    [identity_Id]               BIGINT     IDENTITY (1, 1) NOT NULL,
    [applyId]                   BIGINT     NULL,
    [mappingId]                 BIGINT     NOT NULL,
    [prePaidSum]                FLOAT (53) NULL,
    [viewallocCommission]       FLOAT (53) NULL,
    [viewallocNeedsToPayToShop] FLOAT (53) NOT NULL,
    [mappingType]               TINYINT    NOT NULL,
    CONSTRAINT [PK_ApplyMapping] PRIMARY KEY CLUSTERED ([identity_Id] ASC)
);

