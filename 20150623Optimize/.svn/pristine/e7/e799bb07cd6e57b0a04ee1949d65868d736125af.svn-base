CREATE TABLE [dbo].[ShopVIPFlaseCount] (
    [Id]     INT         IDENTITY (1, 1) NOT NULL,
    [City]   INT         NOT NULL,
    [Date]   VARCHAR (6) NULL,
    [Count]  INT         NOT NULL,
    [Enable] BIT         NOT NULL,
    CONSTRAINT [PK_ShopVIPFlaseCount] PRIMARY KEY CLUSTERED ([Id] ASC)
);

