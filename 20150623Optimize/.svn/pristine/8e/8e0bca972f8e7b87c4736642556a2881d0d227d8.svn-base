using System.Collections.Generic;
using VAGastronomistMobileApp.Model;

namespace VAGastronomistMobileApp.WebPageDll.Services
{
    /// <summary>
    /// 
    /// </summary>
    public class ShopAuthorityEqualityComparer : IEqualityComparer<ShopAuthority>
    {
        public bool Equals(ShopAuthority x, ShopAuthority y)
        {
            return x.Equals(y);
        }

        public int GetHashCode(ShopAuthority obj)
        {
            return obj.GetHashCode();
        }
    }
}